
## 2026-04-04 16:08 Memory Flush

### 核心决策与进展 (Decisions & Progress)
- **OpenClaw 页面重构**：完成了 Notion 核心页面 `Openclaw` (ID: `1c95d407-f8b6-8031-b2a6-e1c6c5a3f1aa`) 的深度整理。采取“Heading 锚点 + 全量内容展示”逻辑，将 API Keys、环境配置、架构演进分类归纳，并保留了 100% 原始信息。
- **领星 ERP (LingXing) 攻坚**：
    - **凭证入库**：账号 `17754926259` / 密码 `shengri1996426` 已持久化至 `TOOLS.md`。
    - **驱动列装**：成功安装 `xdotool`，并完成 `Playwright` 核心库部署。
    - **技术瓶颈**：受限于 PVE 虚拟化环境的 GPU 加速缺失及领星反爬机制（人机验证/扫码），自动化登录出现超时。
    - **协作共识**：确立“用户手动登录 -> 汐灵 CDP 接管 (connect_over_cdp) -> 自动化爬取”的协作闭环。
- **技能升级**：成功安装并激活 `multi-search-engine` (17 引擎集成)，增强了 Sentinel 的全网扫描能力。

### 关键标识符 (Identifiers)
- **LingXing ERP**: `erp.lingxing.com`
- **CDP Port**: `18800` (Chrome DevTools Protocol)
- **Status**: Playwright Core Ready, Binary Downloading (Slow).

### 风险与心跳 (Risk & Heartbeat)
- **META 风险监控**：已在 Notion `📊 汐灵博弈库` 中录入首条 Gamma 负向溢出预警。
- **代理状态**：`192.168.100.183:7890` 存在 TLS 波动，已制定 7892 备用切换计划。

## 2026-04-04 20:17 (Asia/Shanghai) - 领星 ERP 自动化登录专项记忆

### 1. 进展与状态 (Progress & Status)
- **Playwright 环境**: 已完全就绪。Chromium 二进制文件已下载至 `~/.cache/ms-playwright/` 并验证可用。
- **登录尝试**: 针对 `https://erp.lingxing.com/` 发起了多轮自动化登录。
    - **账号**: `17754926259` / `shengri1996426`。
    - **技术路径**: 经历了 `Playwright (headed)` -> `xdotool (模拟敲击)` -> `坐标精准点击` 的迭代。
- **当前瓶颈**: Ubuntu 系统进入锁屏/休眠状态（ScreenSaver Active），导致物理层面的 `xdotool` 点击无法穿透至网页按钮。

### 2. 关键决策与参数 (Key Decisions & Parameters)
- **坐标校准**: 锁定登录按钮在 1920x1080 分辨率下的物理坐标约为 `(1095, 645)` 或 `(1350, 800)`（存在 DPI 缩放偏移，需解锁后复核）。
- **交互策略**: 由于领星可能有滑块验证且对自动化指纹敏感，采用“汐灵自动填表 + 主人手动滑块/点击”的半自动模式最为稳健。

### 3. 后续待办 (Next Steps)
- [ ] **解锁触发**: 主人解锁屏幕后，立即执行 `xdotool` 抢占焦点并尝试最终登录点击。
- [ ] **数据抓取**: 登录成功后，切换至销售看板抓取今日销量数据。
- [ ] **风险对冲**: 恢复对 META 组合的 Gamma 监测。

### 4. 故障排除备忘 (Troubleshooting)
- 若 `xdotool` 点击无效，需检查 `DISPLAY=:0` 是否正确导出，以及窗口是否被最小化。
- 代理 `http://192.168.100.183:7890` 在系统长期休眠后可能需要重启（`curl -I google.com` 验证）。
### 2026-04-04 21:54 (Session Memory Flush)

**1. 任务执行状态 (Task Status):**
- **目标**: 自动登录领星 ERP (erp.lingxing.com) 并处理登录验证码。
- **状态**: 成功登录。当前页面为领星 ERP 首页。
- **凭证信息**: 见 `TOOLS.md`（已持久化）。

**2. 关键纠错与教训 (Key Corrections & Learnings):**
- **验证码处理缺陷**: 在处理“按顺序点击”图形验证码时，曾出现重试逻辑死循环（反复执行相同的错误选择器命令）。
- **标准流程修正**:
    - **识别**: 必须先截图，调用 `image` 工具识别图片内容（如“请依次点击：猫、狗、鸟”）。
    - **执行**: 计算坐标，按顺序模拟点击，而非仅依赖 DOM 元素。
    - **风控**: 必须设置最大重试次数（建议 3 次），超过则主动停止并请求人工干预，严禁陷入死循环。

**3. 环境信息 (Environment):**
- 浏览器代理: 192.168.100.183:7890 (已在 `TOOLS.md` 中记录)
- 登录账号: 17754926259
