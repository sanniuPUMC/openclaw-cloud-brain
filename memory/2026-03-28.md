### 2026-03-28 领星 ERP 自动化执行记录

- **任务背景**：尝试在 PVE Linux 宿主机的有头模式（DISPLAY=:0）下自动登录领星 ERP 并提取近 7 天广告报表。
- **环境挑战**：
    - PVE VNC 桌面与系统 X11 会话存在隔离，导致初始启动的浏览器在用户视野中不可见。
    - 领星 ERP 具备极强的滑动验证码检测（拼图验证），包含滑动轨迹和鼠标平滑度分析。
- **关键决策与操作**：
    - **显示修复**：通过强制注入 `Xauthority` 凭证（`/run/user/1000/gdm/Xauthority`）并指定 `DISPLAY=:0`，配合 `--window-position=0,0` 和关闭 GPU 加速，成功在 VNC 物理桌面上唤起 Chrome 窗口。
    - **混合自动化策略**：由于领星严厉的自动化行为检测，确立了“AI 自动填表 + 人类辅助滑块”的协作模式。
    - **备选方案探索**：曾尝试部署 `noVNC + Xvfb` 的 Web 监视方案，但因系统 `SIGKILL` 限制和权限冲突未能成功。
- **当前状态**：
    - 浏览器已成功在 VNC 桌面（tty2）弹出并加载领星登录页。
    - 账号 `17754926259` 和密码 `Shengri1996426` 已填入。
    - 目前停留在滑动验证码阶段，等待用户手动滑动通过，一旦检测到进入 `/main` 路径将立即接管后续数据抓取。
- **待办项目**：
    - 登录成功后，自动跳转至广告报表页提取近 7 天数据。
    - 整理数据并生成广告汇总分析报告。
